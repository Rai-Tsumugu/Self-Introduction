services:
  self-introduction:
    build:
      context: ./self-introduction
      dockerfile: Dockerfile
    image: self-introduction/app:latest
    container_name: self-introduction
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
    restart: unless-stopped
